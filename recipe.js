const button=document.querySelector('#copy-code');const code=document.querySelector('#recipe-code');const status=document.querySelector('#copy-status');if(button&&code&&status){button.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(code.textContent.trim());status.textContent=`Recipe code ${code.textContent.trim()} copied`;button.textContent='Copied';}catch{status.textContent='Select and copy the recipe code above.';}});}